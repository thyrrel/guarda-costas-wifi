android {
    compileSdkVersion 34
    ndkVersion flutter.ndkVersion

    compactionOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId ="com.thyrrel.guardacostaswifi"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../../../../../flutter/packages/flutter_tools/gradle/src/main/groovy/flutter.groovy'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$"
}